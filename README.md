# 🟡 Pikachu CNN – Image Classification Using Convolutional Neural Networks

This project is a **binary image classifier** built using a Convolutional Neural Network (CNN) in TensorFlow/Keras.  
The model predicts whether an input image belongs to **Pikachu** or **Not Pikachu** (or any two classes trained).

---

## 📌 Project Overview

- Built a custom CNN from scratch  
- Used **Conv2D**, **MaxPooling2D**, **Flatten**, **Dense**, and **Dropout**  
- Implemented **data augmentation** for better accuracy  
- Trained on a custom dataset containing **two classes**  
- Evaluated performance using **accuracy** and **loss**  
- Included code for **single image prediction**

---

## 🧠 Model Architecture

The model architecture includes:

- **2× Convolutional Layers**  
- **2× MaxPooling Layers**  
- **Flatten Layer**  
- **Dense Layer (128 units, ReLU)**  
- **Dropout (0.5)**  
- **Output Layer (Sigmoid activation)**

**Loss Function:** Binary Crossentropy  
**Optimizer:** Adam  
**Metric:** Accuracy

---



